const Price = ({
  pure_price,
  discount_price,
}: {
  pure_price: number;
  discount_price: number;
}) => {
  console.log(pure_price, discount_price);

  return <div className="">Price</div>;
};

export default Price;
